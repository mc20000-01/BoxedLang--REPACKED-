// Auto-generated by transpilebx.py --lang rust from os.bx. Do not edit by hand.
// NOTE: non-numeric say/wait durations default to 0 rather than erroring
// (a deliberate simplification). Not compile-tested in a live sandbox -
// paste back any rustc/cargo error and it'll get fixed quickly.
use std::cell::{Cell, RefCell};
use std::collections::HashMap;

fn resolve(boxes: &HashMap<String, String>, val: &str) -> String {
    let mut val = val.to_string();
    for _ in 0..2 {
        let mut keys: Vec<&String> = boxes.keys().collect();
        keys.sort_by(|a, b| b.len().cmp(&a.len()));
        for k in keys {
            let pat = format!("${}", k);
            val = val.replace(&pat, &boxes[k]);
        }
    }
    // \/ escapes the character right after it (shields a literal ':'
    // through the blanket strip below).
    let chars: Vec<char> = val.chars().collect();
    let mut out = String::new();
    let mut i = 0;
    while i < chars.len() {
        if chars[i] == '\\' && i + 1 < chars.len() && chars[i + 1] == '/' && i + 2 < chars.len() {
            let c = chars[i + 2];
            out.push(if c == ':' { '\u{0}' } else { c });
            i += 3;
        } else {
            out.push(chars[i]);
            i += 1;
        }
    }
    let out: String = out.chars().filter(|&c| c != ':').collect();
    out.replace('\u{0}', ":")
}

fn test_op(boxes: &HashMap<String, String>, left: &str, right: &str, op: &str) -> bool {
    let left = resolve(boxes, left);
    let right = resolve(boxes, right);
    match op {
        "==" => left == right,
        "!=" => left != right,
        _ => {
            let l: Result<f64, _> = left.parse();
            let r: Result<f64, _> = right.parse();
            if let (Ok(l), Ok(r)) = (l, r) {
                match op {
                    ">" => l > r,
                    "<" => l < r,
                    ">=" => l >= r,
                    "<=" => l <= r,
                    _ => false,
                }
            } else {
                false
            }
        }
    }
}

fn is_numeric(s: &str) -> bool {
    let t = s.strip_prefix('-').unwrap_or(s);
    !t.is_empty() && t.chars().all(|c| c.is_ascii_digit())
}

fn math_op(boxes: &HashMap<String, String>, left: &str, right: &str, op: &str) -> i64 {
    let lv = resolve(boxes, left);
    let rv = resolve(boxes, right);
    let op = resolve(boxes, op);
    let l: i64 = if is_numeric(&lv) { lv.parse().unwrap_or(0) } else { 0 };
    let r: i64 = if is_numeric(&rv) { rv.parse().unwrap_or(0) } else { 0 };
    match op.as_str() {
        "+" => l + r,
        "-" => l - r,
        "*" | "x" => l * r,
        "/" => if r != 0 { l / r } else { 0 },
        "%" => if r != 0 { l % r } else { 0 },
        _ => 0,
    }
}

fn main() {
    let boxes: RefCell<HashMap<String, String>> = RefCell::new(HashMap::new());
    let marks: HashMap<String, i64> = {
        let mut m = HashMap::new();
    m.insert("app-calc".to_string(), 14i64);
    m.insert("app-note".to_string(), 21i64);
    m.insert("app-notes".to_string(), 22i64);
    m.insert("notes-w".to_string(), 25i64);
    m.insert("notes-r".to_string(), 31i64);
    m.insert("app-file".to_string(), 39i64);
    m.insert("app-files".to_string(), 40i64);
    m.insert("files-r".to_string(), 43i64);
    m.insert("files-w".to_string(), 51i64);
    m.insert("files-d".to_string(), 57i64);
    m.insert("app-end".to_string(), 65i64);
    m.insert("boot".to_string(), 67i64);
    m.insert("loggedin".to_string(), 77i64);
    m.insert("loggedfail".to_string(), 82i64);
    m.insert("app-.test".to_string(), 86i64);
    m.insert("test".to_string(), 86i64);
        m
    };
    let pc = Cell::new(0i64);
    let call_stack: RefCell<Vec<i64>> = RefCell::new(Vec::new());
    const END: i64 = 89;

    let result = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        loop {
            let cur = pc.get();
            if cur < 0 || cur >= END { break; }
            match cur {
                0 => {
                    let _k = resolve(&boxes.borrow(), "prm");
                    let _v = resolve(&boxes.borrow(), " \\/:>");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                1 => {
                    // import already merged: app-
                    pc.set(pc.get() + 1);
                    continue;
                }
                2 => {
                    let _v = resolve(&boxes.borrow(), "booting boxed-os  ver 0.01 sky");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "1");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                3 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "login");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "login");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                4 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "password");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "password");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                5 => {
                    let _k = resolve(&boxes.borrow(), "file-usr");
                    let _v = resolve(&boxes.borrow(), "usr: $login   pwd: $password");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                6 => {
                    print!("\x1b[2J\x1b[H");
                    use std::io::Write as _;
                    std::io::stdout().flush().ok();
                    pc.set(pc.get() + 1);
                    continue;
                }
                7 => {
                    let _passed = test_op(&boxes.borrow(), "$login", "admin", "==");
                    let _k = resolve(&boxes.borrow(), "lg");
                    let _v = if _passed { resolve(&boxes.borrow(), "1") } else { resolve(&boxes.borrow(), "0") };
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                8 => {
                    let _passed = test_op(&boxes.borrow(), "$password", "1234", "==");
                    let _k = resolve(&boxes.borrow(), "in");
                    let _v = if _passed { resolve(&boxes.borrow(), "1") } else { resolve(&boxes.borrow(), "0") };
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                9 => {
                    let _k = resolve(&boxes.borrow(), "loggedin");
                    let _v = math_op(&boxes.borrow(), "$lg", "$in", "+").to_string();
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                10 => {
                    if test_op(&boxes.borrow(), "$loggedin", "2", "==") {
                        let _t = resolve(&boxes.borrow(), "loggedin");
                        if "m" == "m" {
                            pc.set(*marks.get(&_t).unwrap_or(&0));
                        } else {
                            pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                        }
                        continue;
                    }
                    pc.set(pc.get() + 1);
                    continue;
                }
                11 => {
                    let _v = resolve(&boxes.borrow(), "bad login or error");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                12 => {
                    let _v = resolve(&boxes.borrow(), "ending os loop");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                13 => {
                    if test_op(&boxes.borrow(), "$loggedin", "2", "!=") {
                        let _t = resolve(&boxes.borrow(), "loggedfail");
                        if "m" == "m" {
                            pc.set(*marks.get(&_t).unwrap_or(&0));
                        } else {
                            pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                        }
                        continue;
                    }
                    pc.set(pc.get() + 1);
                    continue;
                }
                14 => {
                    // mark: app-calc
                    pc.set(pc.get() + 1);
                    continue;
                }
                15 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "#1");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "#1");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                16 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "op");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "op");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                17 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "#2");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "#2");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                18 => {
                    let _k = resolve(&boxes.borrow(), "calc");
                    let _v = math_op(&boxes.borrow(), "$#1", "$#2", "$op").to_string();
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                19 => {
                    let _v = resolve(&boxes.borrow(), "$calc");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                20 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                21 => {
                    // mark: app-note
                    pc.set(pc.get() + 1);
                    continue;
                }
                22 => {
                    // mark: app-notes
                    pc.set(pc.get() + 1);
                    continue;
                }
                23 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "write(w)/read(r) ?");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "?");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                24 => {
                    let _t = resolve(&boxes.borrow(), "notes-:$?");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                25 => {
                    // mark: notes-w
                    pc.set(pc.get() + 1);
                    continue;
                }
                26 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "note name");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "name");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                27 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "note");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "note");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                28 => {
                    let _k = resolve(&boxes.borrow(), "note-:$name");
                    let _v = resolve(&boxes.borrow(), "$note");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                29 => {
                    let _v = resolve(&boxes.borrow(), "done writing");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                30 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                31 => {
                    // mark: notes-r
                    pc.set(pc.get() + 1);
                    continue;
                }
                32 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "name");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "name");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                33 => {
                    let _k = resolve(&boxes.borrow(), "name");
                    let _v = resolve(&boxes.borrow(), "note-:$name");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                34 => {
                    let _v = resolve(&boxes.borrow(), "");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                35 => {
                    let _v = resolve(&boxes.borrow(), "-----------:$name:-----------");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                36 => {
                    let _v = resolve(&boxes.borrow(), "$$name");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                37 => {
                    let _v = resolve(&boxes.borrow(), "");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                38 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                39 => {
                    // mark: app-file
                    pc.set(pc.get() + 1);
                    continue;
                }
                40 => {
                    // mark: app-files
                    pc.set(pc.get() + 1);
                    continue;
                }
                41 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "read(r)/write(w)/del(d) ?");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "?");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                42 => {
                    let _t = resolve(&boxes.borrow(), "files-:$?");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                43 => {
                    // mark: files-r
                    pc.set(pc.get() + 1);
                    continue;
                }
                44 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "name");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "name");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                45 => {
                    let _k = resolve(&boxes.borrow(), "name");
                    let _v = resolve(&boxes.borrow(), "file-:$name");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                46 => {
                    let _v = resolve(&boxes.borrow(), "");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                47 => {
                    let _v = resolve(&boxes.borrow(), "-----------:$name:-----------");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                48 => {
                    let _v = resolve(&boxes.borrow(), "$$name");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                49 => {
                    let _v = resolve(&boxes.borrow(), "");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                50 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                51 => {
                    // mark: files-w
                    pc.set(pc.get() + 1);
                    continue;
                }
                52 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "name");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "name");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                53 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "data");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "data");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                54 => {
                    let _k = resolve(&boxes.borrow(), "file-:$name");
                    let _v = resolve(&boxes.borrow(), "$data");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                55 => {
                    let _v = resolve(&boxes.borrow(), "done writing");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                56 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                57 => {
                    // mark: files-d
                    pc.set(pc.get() + 1);
                    continue;
                }
                58 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "name");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "name");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                59 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "are you sure yes(y)/no(n) ?");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "?");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                60 => {
                    let _k = resolve(&boxes.borrow(), "name");
                    let _v = resolve(&boxes.borrow(), "file-:$name");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                61 => {
                    if test_op(&boxes.borrow(), "$?", "n", "==") {
                        let _t = resolve(&boxes.borrow(), "boot");
                        if "m" == "m" {
                            pc.set(*marks.get(&_t).unwrap_or(&0));
                        } else {
                            pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                        }
                        continue;
                    }
                    pc.set(pc.get() + 1);
                    continue;
                }
                62 => {
                    if test_op(&boxes.borrow(), "$?", "y", "==") {
                        let _k = resolve(&boxes.borrow(), "$name");
                        boxes.borrow_mut().remove(&_k);
                        pc.set(pc.get() + 1);
                        continue;
                    }
                    pc.set(pc.get() + 1);
                    continue;
                }
                63 => {
                    let _v = resolve(&boxes.borrow(), "deleted $name");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                64 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                65 => {
                    // mark: app-end
                    pc.set(pc.get() + 1);
                    continue;
                }
                66 => {
                    std::process::exit(0);
                }
                67 => {
                    // mark: boot
                    pc.set(pc.get() + 1);
                    continue;
                }
                68 => {
                    let _suffix = boxes.borrow().get("prm").cloned().unwrap_or_else(|| "\n:> ".to_string());
                    let _prompt = resolve(&boxes.borrow(), "name of app");
                    print!("{}{}", _prompt, _suffix);
                    use std::io::Write;
                    std::io::stdout().flush().ok();
                    let mut _line = String::new();
                    std::io::stdin().read_line(&mut _line).ok();
                    let _line = _line.trim_end_matches(['\n', '\r']).to_string();
                    let _k = resolve(&boxes.borrow(), "app");
                    boxes.borrow_mut().insert(_k, _line);
                    pc.set(pc.get() + 1);
                    continue;
                }
                69 => {
                    let _t = resolve(&boxes.borrow(), "app-$app");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                70 => {
                    let _k = resolve(&boxes.borrow(), "name");
                    let _v = resolve(&boxes.borrow(), "");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                71 => {
                    let _k = resolve(&boxes.borrow(), "data");
                    let _v = resolve(&boxes.borrow(), "");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                72 => {
                    let _k = resolve(&boxes.borrow(), "note");
                    let _v = resolve(&boxes.borrow(), "");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                73 => {
                    let _k = resolve(&boxes.borrow(), "?");
                    let _v = resolve(&boxes.borrow(), "");
                    boxes.borrow_mut().insert(_k, _v);
                    pc.set(pc.get() + 1);
                    continue;
                }
                74 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                75 => {
                    let _v = resolve(&boxes.borrow(), "error loop ended");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                76 => {
                    std::process::exit(0);
                }
                77 => {
                    // mark: loggedin
                    pc.set(pc.get() + 1);
                    continue;
                }
                78 => {
                    let _v = resolve(&boxes.borrow(), "");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                79 => {
                    let _v = resolve(&boxes.borrow(), "welcome $login");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                80 => {
                    let _v = resolve(&boxes.borrow(), "");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                81 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                82 => {
                    // mark: loggedfail
                    pc.set(pc.get() + 1);
                    continue;
                }
                83 => {
                    let _v = resolve(&boxes.borrow(), "loggin fail");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                84 => {
                    std::process::exit(0);
                }
                85 => {
                    std::process::exit(0);
                }
                86 => {
                    // mark: test
                    pc.set(pc.get() + 1);
                    continue;
                }
                87 => {
                    let _v = resolve(&boxes.borrow(), "test");
                    println!("{}", _v);
                    let _tv_s = resolve(&boxes.borrow(), "0");
                    let _tv: i64 = _tv_s.trim().parse().unwrap_or(0);
                    if _tv > 0 { std::thread::sleep(std::time::Duration::from_secs(_tv as u64)); }
                    pc.set(pc.get() + 1);
                    continue;
                }
                88 => {
                    let _t = resolve(&boxes.borrow(), "boot");
                    if "m" == "m" {
                        pc.set(*marks.get(&_t).unwrap_or(&0));
                    } else {
                        pc.set(_t.parse::<i64>().unwrap_or(1) - 1);
                    }
                    continue;
                }
                _ => break,
            }
        }
    }));
    if let Err(e) = result {
        let msg = if let Some(s) = e.downcast_ref::<&str>() {
            s.to_string()
        } else if let Some(s) = e.downcast_ref::<String>() {
            s.clone()
        } else {
            "unknown panic".to_string()
        };
        let bar = "=".repeat(60);
        eprintln!("\n{}", bar);
        eprintln!("BoxedLANG (transpiled) runtime error at pc={}", pc.get());
        eprintln!("  panic: {}", msg);
        eprintln!("  boxes : {:?}", boxes.borrow());
        eprintln!("  marks : {:?}", marks);
        eprintln!("{}", bar);
        std::process::exit(1);
    }
}
