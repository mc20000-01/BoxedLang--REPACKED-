// Auto-generated by transpilebx.py --lang go from os.bx. Do not edit by hand.
// NOTE: non-numeric say/wait durations default to 0 rather than erroring
// (a deliberate simplification).
package main

import (
    "bufio"
    "fmt"
    "os"
    "sort"
    "strconv"
    "strings"
    "time"
)

var boxes = map[string]string{}
var marks = map[string]int{
    "app-calc": 14,
    "app-note": 21,
    "app-notes": 22,
    "notes-w": 25,
    "notes-r": 31,
    "app-file": 39,
    "app-files": 40,
    "files-r": 43,
    "files-w": 51,
    "files-d": 57,
    "app-end": 65,
    "boot": 67,
    "loggedin": 77,
    "loggedfail": 82,
    "app-.test": 86,
    "test": 86,
}
var pc = 0
var callStack []int
var stdinReader = bufio.NewReader(os.Stdin)

func resolve(val string) string {
    for i := 0; i < 2; i++ {
        keys := make([]string, 0, len(boxes))
        for k := range boxes {
            keys = append(keys, k)
        }
        sort.Slice(keys, func(a, b int) bool { return len(keys[a]) > len(keys[b]) })
        for _, k := range keys {
            val = strings.ReplaceAll(val, "$"+k, boxes[k])
        }
    }
    // \\/ escapes the character right after it (shields a literal ':'
    // through the blanket strip below). Index is advanced explicitly
    // (no implicit for-loop increment) so the 1-vs-3-rune skip per
    // branch stays unambiguous.
    var b strings.Builder
    r := []rune(val)
    i := 0
    for i < len(r) {
        if r[i] == '\\' && i+2 < len(r) && r[i+1] == '/' {
            c := r[i+2]
            if c == ':' {
                b.WriteRune('\x00')
            } else {
                b.WriteRune(c)
            }
            i += 3
        } else {
            b.WriteRune(r[i])
            i++
        }
    }
    val = b.String()
    val = strings.ReplaceAll(val, ":", "")
    val = strings.ReplaceAll(val, "\x00", ":")
    return val
}

func testOp(leftRaw, rightRaw, op string) bool {
    left := resolve(leftRaw)
    right := resolve(rightRaw)
    switch op {
    case "==":
        return left == right
    case "!=":
        return left != right
    }
    l, errL := strconv.ParseFloat(left, 64)
    r, errR := strconv.ParseFloat(right, 64)
    if errL != nil || errR != nil {
        return false
    }
    switch op {
    case ">":
        return l > r
    case "<":
        return l < r
    case ">=":
        return l >= r
    case "<=":
        return l <= r
    }
    return false
}

func isNumeric(s string) bool {
    t := strings.TrimPrefix(s, "-")
    if t == "" {
        return false
    }
    for _, c := range t {
        if c < '0' || c > '9' {
            return false
        }
    }
    return true
}

func mathOp(leftRaw, rightRaw, opRaw string) int {
    lv := resolve(leftRaw)
    rv := resolve(rightRaw)
    op := resolve(opRaw)
    l := 0
    if isNumeric(lv) { l, _ = strconv.Atoi(lv) }
    r := 0
    if isNumeric(rv) { r, _ = strconv.Atoi(rv) }
    switch op {
    case "+":
        return l + r
    case "-":
        return l - r
    case "*", "x":
        return l * r
    case "/":
        if r != 0 { return l / r }
        return 0
    case "%":
        if r != 0 { return l % r }
        return 0
    }
    return 0
}

func run() {
    const END = 89
    for pc < END {
        switch pc {
        case 0:
            boxes[resolve("prm")] = resolve(" \\/:>")
            pc++
            continue
        case 1:
            _ = 0 // import already merged: app-
            pc++
            continue
        case 2:
            fmt.Println(resolve("booting boxed-os  ver 0.01 sky"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("1")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 3:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("login") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("login")] = _line
            pc++
            continue
        case 4:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("password") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("password")] = _line
            pc++
            continue
        case 5:
            boxes[resolve("file-usr")] = resolve("usr: $login   pwd: $password")
            pc++
            continue
        case 6:
            fmt.Print("\x1b[2J\x1b[H")
            pc++
            continue
        case 7:
            if testOp("$login", "admin", "==") {
                boxes[resolve("lg")] = resolve("1")
            } else {
                boxes[resolve("lg")] = resolve("0")
            }
            pc++
            continue
        case 8:
            if testOp("$password", "1234", "==") {
                boxes[resolve("in")] = resolve("1")
            } else {
                boxes[resolve("in")] = resolve("0")
            }
            pc++
            continue
        case 9:
            boxes[resolve("loggedin")] = strconv.Itoa(mathOp("$lg", "$in", "+"))
            pc++
            continue
        case 10:
            if testOp("$loggedin", "2", "==") {
                _t := resolve("loggedin")
                if "m" == "m" {
                    if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
                } else {
                    n, _ := strconv.Atoi(_t)
                    pc = n - 1
                }
                continue
            }
            pc++
            continue
        case 11:
            fmt.Println(resolve("bad login or error"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 12:
            fmt.Println(resolve("ending os loop"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 13:
            if testOp("$loggedin", "2", "!=") {
                _t := resolve("loggedfail")
                if "m" == "m" {
                    if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
                } else {
                    n, _ := strconv.Atoi(_t)
                    pc = n - 1
                }
                continue
            }
            pc++
            continue
        case 14:
            _ = 0 // mark: app-calc
            pc++
            continue
        case 15:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("#1") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("#1")] = _line
            pc++
            continue
        case 16:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("op") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("op")] = _line
            pc++
            continue
        case 17:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("#2") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("#2")] = _line
            pc++
            continue
        case 18:
            boxes[resolve("calc")] = strconv.Itoa(mathOp("$#1", "$#2", "$op"))
            pc++
            continue
        case 19:
            fmt.Println(resolve("$calc"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 20:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 21:
            _ = 0 // mark: app-note
            pc++
            continue
        case 22:
            _ = 0 // mark: app-notes
            pc++
            continue
        case 23:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("write(w)/read(r) ?") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("?")] = _line
            pc++
            continue
        case 24:
            _t := resolve("notes-:$?")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 25:
            _ = 0 // mark: notes-w
            pc++
            continue
        case 26:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("note name") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("name")] = _line
            pc++
            continue
        case 27:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("note") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("note")] = _line
            pc++
            continue
        case 28:
            boxes[resolve("note-:$name")] = resolve("$note")
            pc++
            continue
        case 29:
            fmt.Println(resolve("done writing"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 30:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 31:
            _ = 0 // mark: notes-r
            pc++
            continue
        case 32:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("name") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("name")] = _line
            pc++
            continue
        case 33:
            boxes[resolve("name")] = resolve("note-:$name")
            pc++
            continue
        case 34:
            fmt.Println(resolve(""))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 35:
            fmt.Println(resolve("-----------:$name:-----------"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 36:
            fmt.Println(resolve("$$name"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 37:
            fmt.Println(resolve(""))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 38:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 39:
            _ = 0 // mark: app-file
            pc++
            continue
        case 40:
            _ = 0 // mark: app-files
            pc++
            continue
        case 41:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("read(r)/write(w)/del(d) ?") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("?")] = _line
            pc++
            continue
        case 42:
            _t := resolve("files-:$?")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 43:
            _ = 0 // mark: files-r
            pc++
            continue
        case 44:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("name") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("name")] = _line
            pc++
            continue
        case 45:
            boxes[resolve("name")] = resolve("file-:$name")
            pc++
            continue
        case 46:
            fmt.Println(resolve(""))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 47:
            fmt.Println(resolve("-----------:$name:-----------"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 48:
            fmt.Println(resolve("$$name"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 49:
            fmt.Println(resolve(""))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 50:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 51:
            _ = 0 // mark: files-w
            pc++
            continue
        case 52:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("name") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("name")] = _line
            pc++
            continue
        case 53:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("data") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("data")] = _line
            pc++
            continue
        case 54:
            boxes[resolve("file-:$name")] = resolve("$data")
            pc++
            continue
        case 55:
            fmt.Println(resolve("done writing"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 56:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 57:
            _ = 0 // mark: files-d
            pc++
            continue
        case 58:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("name") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("name")] = _line
            pc++
            continue
        case 59:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("are you sure yes(y)/no(n) ?") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("?")] = _line
            pc++
            continue
        case 60:
            boxes[resolve("name")] = resolve("file-:$name")
            pc++
            continue
        case 61:
            if testOp("$?", "n", "==") {
                _t := resolve("boot")
                if "m" == "m" {
                    if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
                } else {
                    n, _ := strconv.Atoi(_t)
                    pc = n - 1
                }
                continue
            }
            pc++
            continue
        case 62:
            if testOp("$?", "y", "==") {
                delete(boxes, resolve("$name"))
                pc++
                continue
            }
            pc++
            continue
        case 63:
            fmt.Println(resolve("deleted $name"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 64:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 65:
            _ = 0 // mark: app-end
            pc++
            continue
        case 66:
            os.Exit(0)
        case 67:
            _ = 0 // mark: boot
            pc++
            continue
        case 68:
            _suffix := "\n:> "
            if v, ok := boxes["prm"]; ok { _suffix = v }
            fmt.Print(resolve("name of app") + _suffix)
            _line, _ := stdinReader.ReadString('\n')
            _line = strings.TrimRight(_line, "\r\n")
            boxes[resolve("app")] = _line
            pc++
            continue
        case 69:
            _t := resolve("app-$app")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 70:
            boxes[resolve("name")] = resolve("")
            pc++
            continue
        case 71:
            boxes[resolve("data")] = resolve("")
            pc++
            continue
        case 72:
            boxes[resolve("note")] = resolve("")
            pc++
            continue
        case 73:
            boxes[resolve("?")] = resolve("")
            pc++
            continue
        case 74:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 75:
            fmt.Println(resolve("error loop ended"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 76:
            os.Exit(0)
        case 77:
            _ = 0 // mark: loggedin
            pc++
            continue
        case 78:
            fmt.Println(resolve(""))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 79:
            fmt.Println(resolve("welcome $login"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 80:
            fmt.Println(resolve(""))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 81:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        case 82:
            _ = 0 // mark: loggedfail
            pc++
            continue
        case 83:
            fmt.Println(resolve("loggin fail"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 84:
            os.Exit(0)
        case 85:
            os.Exit(0)
        case 86:
            _ = 0 // mark: test
            pc++
            continue
        case 87:
            fmt.Println(resolve("test"))
            _tv, _ := strconv.Atoi(strings.TrimSpace(resolve("0")))
            if _tv > 0 { time.Sleep(time.Duration(_tv) * time.Second) }
            pc++
            continue
        case 88:
            _t := resolve("boot")
            if "m" == "m" {
                if v, ok := marks[_t]; ok { pc = v } else { pc = 0 }
            } else {
                n, _ := strconv.Atoi(_t)
                pc = n - 1
            }
            continue
        default:
            return
        }
    }
}
func main() {
    defer func() {
        if r := recover(); r != nil {
            bar := strings.Repeat("=", 60)
            fmt.Fprintln(os.Stderr, "\n"+bar)
            fmt.Fprintf(os.Stderr, "BoxedLANG (transpiled) runtime error at pc=%d\n", pc)
            fmt.Fprintf(os.Stderr, "  panic: %v\n", r)
            fmt.Fprintf(os.Stderr, "  boxes : %v\n", boxes)
            fmt.Fprintf(os.Stderr, "  marks : %v\n", marks)
            fmt.Fprintln(os.Stderr, bar)
            os.Exit(1)
        }
    }()
    run()
}
